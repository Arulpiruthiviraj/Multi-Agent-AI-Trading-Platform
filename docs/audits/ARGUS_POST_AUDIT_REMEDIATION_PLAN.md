# ARGUS Post-Audit Remediation Plan

Source of truth: `docs/audits/ARGUS_CURRENT_STATE_AND_PAPER_READINESS_AUDIT.md` (2026-09-06).
This plan re-verifies each finding against current code/runtime/DB before acting — audit findings
are treated as hypotheses, not accepted truth, per the standing rule for this whole exercise.

**Scoping decision, stated up front:** per the mission's own §28 "Tomorrow-Safe Change Policy," this
plan executes Priority 0 (paper safety/reliability) items that are genuinely boundable before
Monday, and explicitly **defers** the large, multi-day items (Java/TS parity root-cause across every
indicator, full Wyckoff Java implementation, full production-strategy migration to Java) rather than
rushing them — those are named in §28 itself as exactly the kind of change not to force before a
session. Deferred items are listed honestly below, not silently dropped.

| ID | Finding | Severity | Root Cause (re-verified) | Action | Before Monday? | Status |
|---|---|---|---|---|---|---|
| R1 | DEF-27 graceful-shutdown fix uncommitted | P1 | **RESOLVED between audit and remediation start** — re-verified: now committed at `7e9f642` (git log confirms this file's last touch), fix content confirmed present (all 9 workers stopped before `sqliteDb.close()`), test coverage confirmed present (`gracefulShutdown.test.ts` asserts stop-call and stop-before-close ordering). Committed by the concurrent session active in this repo, not by this remediation pass. | No action needed | N/A | **FIXED** (verified, not by this session) |
| R2 | No silent-death observability/supervision | P1 | Confirmed: no process supervisor active; heartbeat exists but no watchdog escalation on gap | Built `src/server/core/heartbeatWatchdog.ts` — an in-process watchdog, NOT a substitute for a real OS-level process supervisor (a crashed process cannot watch itself; that half of R2 remains an operator/ops action — e.g. a Windows service wrapper with auto-restart, pm2, systemd). Detects the complementary failure mode: process alive but an interval worker silently dead. Signal (after ruling out FundamentalAgent/MacroAgent/TechnicalAgent — none tick reliably regardless of Autobot/session state): NewsAgent's heartbeat ticks unconditionally on its own timer; a gap past `heartbeatWatchdogSilenceThresholdMs` (900000ms, config) corroborated by `MarketDataWorker.isConnected()===false` (so a NewsEngine-only glitch alone can't trip it) pauses trading via the existing `setTradingState('TRADING_PAUSED', ...)` path — never a second kill switch, never auto-resumes, one trigger per process lifetime. Wired into `ArgusCoreBoot.ts` (start) and `gracefulShutdown.ts` (stop, before `sqliteDb.close()`, DEF-27-consistent). New config keys added to `tradingSafety.json`/`runtimeIntervals.json` (required-key lists updated in both loaders). 6 new tests (`heartbeatWatchdog.test.ts`, real temp DB + real `tradingEngine.setTradingState`, mocked `MarketDataWorker.isConnected` only) — all passing, stable across repeated runs. `ARGUS_ARCHITECTURE.md` updated in the same change (new subsection). `tsc --noEmit` clean. **L2 TEST proof reached; L3 RUNTIME (a real restart + observing it stay quiet in normal operation) still pending.** | YES | **Code + tests done; runtime verification pending** |
| R3 | MISSING_PRICE = 100% of TRADE_IDEA_REJECTED, now FundamentalAgent/MacroAgent-sourced | P1 | Audit found NewsAgent's race was fixed but Fundamental/Macro producers were never given the same fresh-price-await treatment | `FundamentalAgent.ts`/`MacroAgent.ts` now call `waitForFreshMarketData()` (identical pattern to NewsEngine.ts) before proceeding; fail-closed DATA_UNAVAILABLE HOLD, never a fabricated price. Regression tests added/rewritten in both `.test.ts` files (mocking `waitForFreshMarketData` directly, not its internal `requestTemporaryDataRescue`/`getLatestPrice` dependencies, to avoid a real cross-test `inFlight`-Map leak this exposed in the real module — see the tests' own header comments). `tsc --noEmit` clean; both files green, 41/41, stable across 5 repeated combined runs. **L2 TEST proof reached; L3 RUNTIME/L4 PAPER SESSION still pending an actual restart + live tick.** | YES | **FIXED (code + tests); pending runtime verification** |
| R4 | IBKR market-data inconsistency (`marketDataConnected:false` while socket CONNECTED) | P1 | Audit classified UNKNOWN — weekend-normal vs. real bug not distinguished | **Diagnostic-logging half done**: `ArgusRuntime.health()`/`ArgusRuntimeSnapshot` exposed only the bare `marketDataConnected` boolean — an operator seeing it flip false had no way to tell "socket never authenticated" from "a real lastError (e.g. IBKR error 354)" from "raw readyState says something else entirely," which is exactly why the audit itself could only say "cause UNKNOWN." Added `marketDataAuthenticated`/`marketDataLastError`/`marketDataReadyState` to the health payload, sourced from `MarketDataWorker.getFeedStatus()` (already computed, just never surfaced past `.connected`). Zero behavior change - purely additive fields. 1 new assertion in `ArgusRuntime.test.ts`; `tsc --noEmit` clean. Root cause itself still cannot be diagnosed without a real Monday-open observation - this only ensures the NEXT occurrence carries real evidence instead of a bare boolean. | Partial (diagnostics only) | **Diagnostic fields added; root-cause still deferred to Monday-open observation** |
| R5 | IBKR error 354 (market-data-line entitlement) | P1 | Prior sessions already traced this to an account-entitlement gap, not a code bug | No code fix possible; operator must check IBKR account market-data subscriptions | N/A — operator action | Unresolved, not code-fixable |
| R6 | Chronos/Python resource safety | Resolved in audit | Confirmed RESOLVED (thread count flat, fix intact in code) | Monitor only, no change | N/A | No action needed |
| R7 | Config drift / startup verification | Resolved in audit | No drift found | Monitor only | N/A | No action needed |
| R8 | Java/TS RSI/MACD parity divergence (80% >5%, 56% >20%) | P1 | **Root-caused (2026-09-07, follow-up pass)** — NOT a formula bug: `quant-core-java/src/main/java/io/argus/quantcore/indicators/RSI.java` and `MACD.java` are literal, verified byte-for-byte ports of `src/server/engines/RSIEngine.ts`/`MACDEngine.ts` (confirmed by direct side-by-side code read; Java's own doc comments say so). The real cause is upstream of the math: `QuantCoreBridge.ts`'s `compareParity()` diffs a TS-side snapshot computed from `this.priceHistory[symbol]` (TS's own in-memory array) against a Java-side snapshot computed from `SymbolState.java`'s **independently-accumulated** tick ring buffer (`CAPACITY=200`, fed only by a separate, best-effort, fire-and-forget `POST /api/v1/ticks` per tick — no delivery/ordering guarantee, no periodic full-resync). Any dropped/reordered/delayed tick makes the two arrays diverge in content, and because RSI/MACD use Wilder/EMA smoothing (each value depends recursively on the previous), a single early divergence propagates forward permanently rather than self-correcting like a plain SMA would — exactly matching the observed "large, persistent" divergence shape rather than a small rounding drift. `SymbolState.java`'s own header comment already documents that its history is tick-based, not the same bar series the TS side uses, for ATR/VWAP; this finding extends that same root cause to RSI/MACD/Bollinger, which the comment had described as "the same real computations... over the same price series a real close-price history would produce" — that assumption is what this finding disproves. | **Not fixed this pass** — the real fix (redesign `QuantCoreBridge`'s wire protocol so Java compares against the exact array TS holds, e.g. by sending the full series with each comparison request instead of relying on independent tick accumulation) is a genuine cross-language protocol change to the Node↔Java boundary, not a formula tweak — attempting it under this pass's own time pressure risks exactly the kind of rushed, unproven quant-adjacent change §28 warns against, and CLAUDE.md rule 10 (measure the actual bottleneck, don't guess) applies here too: the fix design itself (full-array payload vs. resync-on-demand vs. sequence-numbered tick delivery) deserves its own dedicated pass. | NO (deferred, but now scoped with a concrete, evidence-backed fix design instead of an open question) | **Root cause found and documented; structural fix deferred as a properly-scoped follow-up** |
| R9 | Complete Java quant ownership / delete TS duplicates | P1→P2 | N/A — this is the multi-week migration program already underway (JMIG-001 etc.) | Continue existing phased approach; no acceleration | NO (deferred) | Ongoing, out of this sprint's scope |
| R10 | Migrate 5 CORE strategies fully to Java | P1→P2 | N/A | Deferred — same reasoning as R8/R9 | NO (deferred) | Deferred |
| R11 | Wyckoff Java implementation | P2 | Confirmed not started (unchanged) | Deferred — this morning's own Phase 0 inventory already scoped this as multi-day; §28 explicitly names "large quant rewrites" as not-to-rush | NO (deferred) | Deferred, Phase 0 inventory already exists |
| R12 | AI provider health (0/10 at audit time) | P1 | External quota/rate-limit state, not a code bug | Verified `AIProviderHealthCheck.ts` code directly: `getAIProviderHealthSnapshot()` always returns `tracker.get(id).lastStatus` from the most recent real `checkProviderHealth()` call (auth + live chat probe) — there is no cache layer serving a stale "healthy" value while checks silently stop running; `recordResult()` correctly overwrites status/consecutiveFailures/error on every tick regardless of previous state. `classifyError()`'s ACCOUNT_SUSPENDED/AUTH_FAILED/QUOTA_EXCEEDED/RATE_LIMITED/MODEL_UNAVAILABLE/PROVIDER_UNAVAILABLE ordering is correct and covered by 15 existing passing tests (`AIProviderHealthCheck.test.ts`). The "0/10 healthy" observation is consistent with a genuine, currently-active external outage/quota state at audit time, not a code defect — matches the original root-cause classification. No code change made (none needed). | Partial (verify check itself) | **VERIFIED — no bug found; monitor only** |
| R13 | MacroAgent/DiagAgent calibration stale since 2026-08-10 | P2 | **Root-caused via direct read-only queries against `data/argus.db`** (engine confirmed not running at query time). Two unrelated causes, not one: (1) `DiagAgent` is a diagnostic test fixture (symbol `DIAGTEST`, 3 rows total, all 2026-08-09) — not a real production agent; its "staleness" is a non-issue, nothing to fix. (2) `MacroAgent` is real and active (4,739 predictions through 2026-09-04) but **100% of them (4,739/4,739) are `HOLD`** — it has never once emitted a real BUY/SELL directional call in this deployment's entire history. `ReflectionEngine.evaluateAgents()`'s own logic is verified CORRECT, not buggy: a HOLD prediction always grades `outcome='N_A'` (confirmed: 1,724/1,724 MacroAgent `prediction_outcomes` rows are `N_A`, vs. `FundamentalAgent`'s same-period mix of 36 WIN / 25 LOSS / 2,917 N_A), so `statsMap['MacroAgent'].total` stays 0 forever and the loop's `if (data.total === 0) continue;` correctly skips writing a new `agent_performance_stats`/`agent_confidence_calibration` row every cycle — the frozen 2026-08-10 timestamp is a downstream *symptom* of MacroAgent's own output, not a broken evaluator. Some HOLD rows carry a real nonzero confidence (0.7, not the 0-confidence `emitHold()` fail-closed pattern), suggesting the real AI call is succeeding and genuinely answering HOLD, not silently falling back on a data/provider failure — but a 100%-HOLD rate across weeks is still unusual enough to warrant a closer look at the macro prompt/coercion path. | Not fixed this pass — root cause is now evidence-based and scoped, not guessed; determining WHY the model/prompt combination never produces a directional call needs a live LLM probe or prompt-engineering review, not a code read, and risks being a rushed, unproven quant-adjacent change under this sprint's own §28 policy. | If time permits | **Root-caused; fix deferred pending live-LLM investigation** |
| R14 | ETF concentration, rescue denial rate | P2, expected-by-design per audit | Already correctly explained (structural, not a bug) | No change — audit explicitly said don't optimize the allocator without more evidence | NO | No action (by design) |
| R15 | TradePlanBuilder / Extended-hours runtime-unproven | Not a bug | Confirmed: zero opportunities yet (weekend), not broken | Monitor Monday, no code change | N/A | No action needed |
| R16 | Full `npm test` run (2026-09-07, post-R2/R3) showed 8 real failures across 4 files (`marketDataToRisk.test.ts`, `paperSpineInternalPaper.test.ts` ×2, `RiskEngine.gates.test.ts`, `RiskEngine.restrictedLive.test.ts`) — not present in CLAUDE.md's last-recorded harness count | P1 (test-suite integrity, not live-spine) | Root-caused via direct debug log: `rejectionGate: "extended_hours_execution_policy"`, `EXTENDED_HOURS_BROKER_UNSUPPORTED`. This deployment's real `.env` has `EXTENDED_HOURS_EXECUTION_ENABLED=true` (per CLAUDE.md's Phase 5 note) and the suite happened to run during real pre-market wall-clock hours (08:11 ET) — gate 25 correctly, by design, rejected every order since `InternalPaperBroker` has no extended-hours order construction. **Not a RiskEngine/gate-25 bug** — a test-hermeticity gap: these 4 files never intended to exercise the extended-hours path and didn't control for real wall-clock time or the developer's real `.env`, unlike `RiskEngine.test.ts`/`OrderManagement.test.ts` which already handle this correctly per-test. | Added one line to `vitest.setup.ts` (same established `ARGUS_TEST_ALLOW_*` opt-in-override idiom already used for OpenAlice/Chronos/Ollama): defaults `EXTENDED_HOURS_EXECUTION_ENABLED='false'` for every test file unless `ARGUS_TEST_ALLOW_EXTENDED_HOURS=true`. Must be an **assignment**, not a `delete` — confirmed live that a `delete` alone gets silently repopulated by a later transitive `dotenv.config()` call (same hazard this file's own OpenAlice comment already documents), which is why the first attempt at this fix didn't work and had to be corrected. Re-verified: all 4 originally-failing files pass, plus `RiskEngine.test.ts`/`OrderManagement.test.ts` (which deliberately set the flag `true` per-test) still pass unaffected — 84/84 across all 6 files. | YES | **FIXED, verified** |
| R17 | `AI_PROVIDERS_EXHAUSTED` events persist with empty payload — minor observability gap (§21, P2) | P2 | **Root-caused precisely**: `instrumentEventBus.ts`'s generic `eventBus.on('*', ...)` bridge (`installObservabilityEventBridge()`) extracts a fixed, trade-idea-shaped field set (`symbol`/`side`/`status`/`gate`/`approved`/`agent`/`confidence`/`orderId`/`stage`) for every event type it observes. `AI_PROVIDERS_EXHAUSTED`'s real payload fields (`agentType`, `lastError`, `providersAttempted`, set by `AIRouter.ts`'s `emitProvidersExhausted()`, confirmed non-empty at the source) match none of those names — every extracted field comes back `undefined`, and `JSON.stringify` silently drops them all, so the row persisted to `observability_events` is genuinely `{}` even though the in-memory event never was. Not a symptom of anything upstream; a real, narrow bug in the generic bridge's field-extraction list. | Added `agentType`/`lastError`/`providersAttempted` to the extracted field set in `instrumentEventBus.ts` (additive — no existing field removed, zero effect on any other event type's payload). New `instrumentEventBus.test.ts` (2 tests): proves `AI_PROVIDERS_EXHAUSTED` now persists real, non-empty fields, and proves the existing trade-idea-shaped extraction still works unchanged. `tsc --noEmit` clean. | If time permits | **FIXED, verified** |

**Explicitly deferred, stated plainly (not hidden):** R8, R9, R10, R11 — the Java/TS parity root-cause investigation, the full production-strategy migration, and the Wyckoff Java build. These are real, valuable, multi-day-to-multi-week efforts that would not survive the "tested and proven before tomorrow" bar this mission itself sets in §28/§29. Attempting them under time pressure risks exactly the kind of unproven, rushed change the audit's own principle warns against.

## Final verification (2026-09-07)

Full suite run to completion twice after all fixes above (R2/R3/R4/R12/R13/R16/R17 combined):
**459 test files / 3,296 tests, 100% passing, zero regressions.** `tsc --noEmit` clean throughout.

Every finding in this plan has now reached one of three honest end states:
1. **Fixed and verified at L2 TEST** (R2, R3, R4 diagnostics, R16, R17) — code + passing regression tests; L3 RUNTIME/L4 PAPER SESSION proof for R2/R3 still requires an actual engine restart and live tick, which this session did not perform (per its own scoping, changes were made and proven in isolation rather than pushed live mid-investigation).
2. **Verified with no code change needed** (R1, R6, R7, R12, R14, R15) — re-checked against current code/runtime/DB and confirmed either already resolved or not a bug.
3. **Explicitly deferred with a stated reason** (R5 — operator/account action, not code; R8-R11 — large multi-day quant/Wyckoff work per the mission's own §28 policy; R13 — root-caused but the underlying "why does the model always answer HOLD" question needs a live LLM probe, not a code read; R4's ultimate root cause — needs a real Monday-open IBKR observation).

No finding was silently dropped or rushed.
