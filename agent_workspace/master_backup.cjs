const fs=require('node:fs');const Database=require('better-sqlite3');
(async()=>{const target='data/backups/pre-master-0070-20260920T0203.db';if(fs.existsSync(target))throw Error('Backup already exists; refusing overwrite');fs.mkdirSync('data/backups',{recursive:true});const db=new Database('data/argus.db',{readonly:true,fileMustExist:true});await db.backup(target);db.close();const copy=new Database(target,{readonly:true,fileMustExist:true});const check=copy.pragma('quick_check');const forecasts=copy.prepare('SELECT count(*) n FROM quant_forecasts').get();copy.close();console.log(JSON.stringify({target,bytes:fs.statSync(target).size,check,forecasts}));})().catch(e=>{console.error(String(e));process.exitCode=1});
